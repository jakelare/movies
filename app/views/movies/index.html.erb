<h1><%= pluralize(@movies.size, 'Movie')%></h1>


  <% @movies.each do |movie| %>
<article>
  <header>
    <strong><%= link_to(movie.name, movie) %></strong>
  </header>
  <p>
    <%= movie.description %>
  </p>
  <table>
    <tr>
      <th>When:</th>
      <td><%= movie.starts_at %></td>
      </tr>
    <tr>
      <th>Where:</th>
      <td><%= movie.location %></td>
      </tr>
    <tr>
      <th>Price:</th>
      <td><%= format_price(movie) %></td>
    </tr>
  </table>
  </article>
  <% end %>



